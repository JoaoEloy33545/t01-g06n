# The CSerDvectorGrpcAPIM
Defines the gRPC service contract in `src/main/proto/dvector.proto`.
The proto file generates Java classes used by gRPC servers and clients.

## Target artifacts
The directory target is generated by the Maven command

mvn install

And it can be removed by

mvn clean
